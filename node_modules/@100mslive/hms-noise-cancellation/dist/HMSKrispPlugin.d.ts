import { EventBus, HMSAudioPlugin, HMSAudioPluginType, HMSPluginSupportResult } from "@100mslive/hms-video-store";
import { AudioFilterNode } from "./krisp";
export declare class HMSKrispPlugin implements HMSAudioPlugin {
    private sdk;
    private readonly TAG;
    private filterNode;
    private eventBus?;
    checkSupport(): HMSPluginSupportResult;
    setEventBus(eventBus: EventBus): void;
    init(): Promise<void>;
    getPluginType(): HMSAudioPluginType;
    getName(): string;
    isSupported(): boolean;
    toggle(): void;
    isEnabled(): boolean | undefined;
    processAudioTrack(ctx: AudioContext, source: AudioNode): Promise<AudioFilterNode>;
    stop(): void;
}
